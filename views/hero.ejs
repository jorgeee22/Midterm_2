<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styleHero.css">
    <title><%= hero.name %> - Superhero Details</title>
</head>
<body>
    <h1>Superhero Details</h1>

    <div class="hero-details">
        <!-- Imagen del personaje -->
        <img src="<%= hero.images.md %>" alt="<%= hero.name %>" class="hero-image">
        
        <!-- ID -->
        <p><strong>ID:</strong> <%= hero.id %></p>
        
        <!-- Nombre y nombre completo -->
        <h2><%= hero.name %></h2>
        <p><strong>Full Name:</strong> <%= hero.biography.fullName %></p>
        
        <!-- Estadísticas de poder -->
        <h3>Power Stats</h3>
        <ul>
            <li><strong>Intelligence:</strong> <%= hero.powerstats.intelligence %></li>
            <li><strong>Strength:</strong> <%= hero.powerstats.strength %></li>
            <li><strong>Speed:</strong> <%= hero.powerstats.speed %></li>
            <li><strong>Durability:</strong> <%= hero.powerstats.durability %></li>
            <li><strong>Power:</strong> <%= hero.powerstats.power %></li>
            <li><strong>Combat:</strong> <%= hero.powerstats.combat %></li>
        </ul>

        <!-- Lugar de nacimiento -->
        <p><strong>Place of Birth:</strong> <%= hero.biography.placeOfBirth %></p>

        <!-- Pequeña biografía -->
        <p><strong>Biography:</strong> <%= hero.biography.fullName ? `Also known as ${hero.biography.fullName}` : 'No biography available.' %></p>

        <!-- Alias -->
        <p><strong>Aliases:</strong> <%= hero.biography.aliases.join(', ') || 'No aliases available.' %></p>

        <!-- Género, raza, altura, peso -->
        <p><strong>Gender:</strong> <%= hero.appearance.gender %></p>
        <p><strong>Race:</strong> <%= hero.appearance.race || 'Unknown' %></p>
        <p><strong>Height:</strong> <%= hero.appearance.height.join(', ') %></p>
        <p><strong>Weight:</strong> <%= hero.appearance.weight.join(', ') %></p>

        <!-- Color de ojos y pelo -->
        <p><strong>Eye Color:</strong> <%= hero.appearance.eyeColor || 'Unknown' %></p>
        <p><strong>Hair Color:</strong> <%= hero.appearance.hairColor || 'Unknown' %></p>

        <!-- Afiliación a un grupo -->
        <p><strong>Group Affiliation:</strong> <%= hero.connections.groupAffiliation || 'None' %></p>
    </div>

    <div class="navigation">
        <a href="/hero/<%= prevHeroId %>" class="btn btn-secondary">Anterior</a>
        <a href="/hero/<%= nextHeroId %>" class="btn btn-secondary">Siguiente</a>
    </div>

    <a href="/" class="btn btn-primary">Volver a la página principal</a> 
</body>
</html>
